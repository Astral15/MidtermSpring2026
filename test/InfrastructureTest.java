import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/**
 * Confirms the JUnit 5 test infrastructure is wired up correctly.
 * Replace / delete once real characterization tests are in place.
 */
class InfrastructureTest {

    @Test
    void junit_infrastructure_works() {
        assertTrue(true, "JUnit 5 is on the classpath and running");
    }
}

